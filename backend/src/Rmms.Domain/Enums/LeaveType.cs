namespace Rmms.Domain.Enums;

/// <summary>Kind of leave (M08). Stored as snake_case string (<c>regular</c> / <c>emergency</c>).</summary>
public enum LeaveType
{
    Regular = 1,
    Emergency = 2,
}
