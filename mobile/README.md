# RMMS Mobile (PG & Leader)

Flutter 3.22+ · Riverpod 2 · Dio · Hive · Freezed · go_router · ARB-based l10n (vi/en).

## Initialize platform folders (one-time)

`android/` and `ios/` are not committed — they get generated by Flutter the first time you set up the project on a new machine:

```bash
cd mobile
flutter create --org com.motivesvn.rmms --project-name rmms .
```

That command only fills missing platform folders; it does NOT overwrite `lib/`, `pubspec.yaml`, `analysis_options.yaml`, or `l10n.yaml`.

## Quickstart

```bash
flutter pub get
dart run build_runner build --delete-conflicting-outputs   # codegen: freezed + riverpod + json
flutter gen-l10n                                            # ARB → AppLocalizations
flutter run --dart-define=API_BASE_URL=http://10.0.2.2:5080
```

For physical Android device, replace `10.0.2.2` with your machine's LAN IP.

## Project Layout

```
lib/
├── main.dart
├── app.dart
├── core/
│   ├── config/app_config.dart
│   ├── network/api_client.dart
│   ├── network/api_interceptors.dart
│   ├── router/app_router.dart
│   ├── storage/secure_storage.dart
│   └── theme/app_theme.dart
├── features/
│   ├── auth/
│   │   ├── data/auth_api.dart
│   │   ├── domain/auth_user.dart
│   │   └── presentation/screens/login_screen.dart
│   └── home/
│       └── presentation/screens/home_screen.dart
└── l10n/
    ├── app_vi.arb
    ├── app_en.arb
    └── generated/   # AppLocalizations.dart (built by `flutter gen-l10n`)
test/
└── widget_test.dart
```

## Conventions

See `../knowledge-base/08-coding-standards.md` (Mobile section).

- Riverpod 2.x for state; one provider per file
- Freezed for immutable models
- ARB-based l10n; never hardcode user-visible strings
- All paths/routes go through `AppRoutes` constants in `core/router/app_router.dart`
- Error codes (not raw messages) drive UX branching — map codes → ARB keys
